CREATE TABLE PRACTICA(ID_PRACTICA INT,EDAD INT,NOMBRE VARCHAR2(50));
BEGIN
FOR I IN 1..300 LOOP
DBMS_OUTPUT.PUT_LINE(I||'CICLO');
INSERT INTO PRACTICA RANDOM.VALUES(I,1..40,'PEDRO'||'ANA'||'JUAN');
END LOOP;
END;

SELECT * FROM REGISTRO;

